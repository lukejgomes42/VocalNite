#pragma once
#include <string>

// =============================================================================
//  TEMPLATE FILE - copy this to "Secrets.h" and fill in real credentials.
//  Secrets.h is gitignored and must never be committed.
// =============================================================================

namespace VocalNiteSecrets
{
    // Supabase PostgreSQL pooler connection string
    inline const std::string kPostgresConnectionString =
        "postgresql://USER:PASSWORD@HOST:PORT/DATABASE";

    // Gmail SMTP credentials for email verification
    inline const std::string kGmailUser     = "your-email@gmail.com";
    inline const std::string kGmailPassword = "xxxx xxxx xxxx xxxx";
}